#include "Manager.h"

int main()
{
    // Create an instance of the Manager class
    Manager manager;

    // Call the 'run' method to start the manager's process
    manager.run();

    return EXIT_SUCCESS;
}